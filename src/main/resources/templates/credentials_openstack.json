{
   "cloudPlatform":"$CLOUD_PLATFORM",
   "name":"$NAME",
   "description": "$DESCRIPTION",
   "publicKey": "$SSHKEY",
   "parameters":{
       "password": "$PASSWORD",
       "tenantName": "$TENANT_NAME",
       "endpoint": "$ENDPOINT"
   }
}
